# xio-ai-assets

Media asset store for Xio AI social content. Files are referenced externally
by their raw URLs at the repository root.

**Please keep this repository flat** — do not move, rename, or reorganize
files into folders; existing published links depend on root-level paths.
Additive changes only.

`_asset_manifest.json` is the machine-readable index of all assets.
